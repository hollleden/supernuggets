# Supernuggets — Backlog

Moved verbatim from CLAUDE.md's "Backlog" section on 2026-07-04 (frontend + bot items were tracked together there). See [CHANGELOG.md](CHANGELOG.md) for shipped-item history and [supernuggets-bot/BACKLOG.md](../supernuggets-bot/BACKLOG.md) for the bot repo's own backlog.

---

#### ✅ TIER 1 — Shipped 2026-05-27

| Item | Notes |
|---|---|
| **Bot commands menu** (`/setcommands` + `/help`) | Registered with Telegram; menu appears on `/` in chat |
| **Voice message handler** (`F.voice → Whisper`) | OGG/OPUS, same pipeline as video. `media_type="voice"` |
| **Card CSS polish** — title `line-clamp-3` + "+N" border box | Prevents grid height blowout on long titles |
| **Tag filtering** — dedicated `?tag=` URL param (exact-match) | NuggetCard tag links → `/?tag=<name>`; chip shows in header |
| **Folder counts** in filter bar (e.g., `GROW · 12`) | Computed from initialNuggets in HomeGrid |
| **Bot link + About** in sidebar | `⬈ @BOT` nav item linking to Telegram bot |

---

#### TIER 2 — Next sprint

| Priority | Item | Notes |
|---|---|---|
| ✅ Done | **Automated digest scheduling** | Verified 2026-07-02 — actually shipped, backlog was stale. `bot.py` wires `AsyncIOScheduler` (`apscheduler==3.11.2` in requirements.txt): `_check_and_send_digests()` runs on an interval job (`DIGEST_CHECK_INTERVAL_MINUTES`), calling `database.get_digest_enabled_users()` and sending weekly/monthly/yearly digests automatically. Gated behind `DIGEST_ENABLED` env var (defaults `"0"` in code) — confirmed set to `1` on Railway, so it is live in prod. Commit `13aa20c` "Add Phase D digest scheduler infrastructure". Manual `/digest_week` etc. commands still work independently. |
| **HIGH** (added 2026-07-02) | **Article extraction (trafilatura) appears completely broken** | Live-tested 2026-07-02: both a NYT article and a Medium article returned `[unsupported] URL_NOT_RECOGNIZED` ("i don't recognize this link — might be behind a paywall, login wall, or heavy JS") instead of extracting body text. Neither URL is paywalled-in-a-way-that-should-fully-block (NYT gives a public lede, Medium is open). Needs investigation: is `extract_article`'s browser-headered fetch getting blocked/redirected, or is `trafilatura.extract` failing to find a body on these page structures? Start by running `pipeline.extract_article()` directly against both URLs outside the bot to see the raw failure. |
| ❌ Dropped 2026-07-03 | **Reddit support — deprioritized, not fixing** | Root-caused 2026-07-03: Reddit links fail completely (not just images) — yt-dlp's Reddit extractor now requires OAuth ("Account authentication is required"; Reddit locked down anonymous API access), and the trafilatura article fallback also fails because Reddit's page is a client-rendered SPA with no server-rendered content to scrape. A working fix would need a registered Reddit API app (free `client_id`/`client_secret` from reddit.com/prefs/apps) — same shape as the `INSTAGRAM_COOKIES` credential pattern. Owner decided it's not worth doing right now. Removed all "supported platforms" claims mentioning Reddit from README.md, docs/SRS.md, docs/ARCHITECTURE.md, and `pipeline.py`/`bot.py` comments + the now-dead `reddit` key in `URL_DURATION_CAPS`. Revisit only if the owner explicitly asks and provides Reddit API credentials. |
| Low (added 2026-07-02) | **Instagram support — re-verify once article extraction is fixed** | Confirmed broken 2026-07-02 in live testing: photo posts hit `INSTAGRAM_PHOTO` (`instagram needs a login to download photos`), reels hit `INSTAGRAM_LOGIN` — both require `INSTAGRAM_COOKIES` to be set, unconfirmed whether it's set on Railway. Not re-prioritizing this on its own, but when `/start` copy is next revisited, check whether cookies got configured and instagram can be added back to the platform list (`bot.py`'s `_build_start_text()` — see BRAND.md rule 9). |
| **HIGH** | **Recipe-video receipt format** (added 2026-06-10) | Recipe / cooking TikToks should get a domain-specific receipt: structured INGREDIENTS list + numbered STEPS section instead of the generic SUMMARY bullets. Likely needs (a) a detector (cue words: "first you'll need", "add the", "mix in", "preheat", measurements like "1 cup" / "2 tbsp"), (b) a new tool schema variant or optional `recipe` field on `save_nugget` with ingredients[] + steps[], (c) a renderer branch in `pipeline.render` that outputs the recipe block in place of SUMMARY when populated. Owner explicitly flagged 2026-06-10 — start by collecting 3-5 real recipe TikToks to design against. |
| **HIGH** | **Native TikTok photo carousel support** | yt-dlp can't extract (JS-rendered). Options: (a) Playwright (slow, fingerprinted), (b) paid API ($10-50/mo, recommended), (c) TikTok mobile API (breaks often), (d) programmatic SaveAsBot. Start with Playwright for dev experience, switch to paid API for reliability. |
| ✅ Shipped 2026-07-02, root-caused + actually fixed 2026-07-03 | **Dark/light mode toggle broken** | The 2026-07-02 "fix" was just pushing already-written commits — the underlying mechanism was still broken and resurfaced. Real root cause: `components/vault/app-shell.tsx`'s mount effect derived theme as `hasDarkClass || systemPrefersDark` and never read `localStorage`, so every page reload discarded the user's choice and fell back to the OS preference. Separately, `app/globals.css` had `dark:` Tailwind utilities (used across 26 component files) and 6 hand-rolled `@media (prefers-color-scheme: dark)` blocks (`.pill-btn`, `.pill-btn-icon`, `.nugget-card`, `.ghost-brutal-btn`, `.bg-dither`) that followed the OS setting directly instead of the `.dark` class — so even parts of a correctly-toggled UI stayed OS-driven. Fixed all three failure points: (1) `app-shell.tsx` now reads only `localStorage.getItem('theme')` on mount, no system fallback at all; (2) `app/layout.tsx` got a blocking inline `<script>` in `<head>` that applies `.dark` from `localStorage` before first paint (prevents a flash of the wrong theme); (3) `globals.css` added `@custom-variant dark (&:where(.dark, .dark *));` so every Tailwind `dark:` class repo-wide now tracks the `.dark` class, and the 6 raw `@media` blocks were converted to `html.dark` selectors. Verified live in the preview browser (toggling `localStorage.theme` between `dark`/`light` + reload) and confirmed by owner on prod after deploy. Commit `397bb8f` on `main`. See memory `project_supernuggets_dark_mode`. |
| ✅ Shipped 2026-07-02 | **PWA home screen icon showing wrong image** | Root cause found: `public/apple-icon.png` (and its two copies `apple-touch-icon.png`/`apple-touch-icon-precomposed.png`) was never actually the pixel nugget sprite — it held a leftover placeholder image (a black "V0" mark, likely from the original scaffold) that predated the 2026-07-02 PWA work. `icon-192.png`/`icon-512.png` (used by the manifest, Android/Chrome) were correct all along; only the iOS-specific apple-touch-icon files were wrong. Regenerated all three from `public/telegram-bot-icon.png` at 180×180 via PIL NEAREST resize (same technique as the other icons). Not a caching issue — the file content itself was wrong. |
| ✅ Shipped 2026-07-02 | **PWA "Add to Home Screen" bookmark pointed at bare root, not the user's vault** | Root cause: the single global `app/manifest.ts` has a static `start_url: '/'`, and iOS 16.4+ honors the linked manifest's `start_url`/`scope` over the current page URL when adding to home screen — so saving from `/u/[token]/...` still installed a shortcut to `/`. Fixed with a per-user manifest: new route `app/u/[token]/manifest.webmanifest/route.ts` returns a manifest with `start_url`/`scope` set to `/u/[token]`, and `app/u/[token]/layout.tsx` now exports `generateMetadata()` that overrides the inherited `manifest` link for every page under that segment. Root `/manifest.webmanifest` (used by the marketing page) is untouched. |
| **HIGH** (added 2026-07-04) | **Sanitize digest body HTML rendering** | Security: `lib/digest-body.tsx:83, 91, 96` use `dangerouslySetInnerHTML` on `body_html` from Supabase without sanitization. Source is defined/trusted (bot-generated via `pipeline.render()`, all user input escaped via `_esc()`), but this is a **high-consequence vector** — if bot escaping ever fails OR database is compromised, arbitrary JS executes on vault page. Mitigation: add `dompurify` (npm, 13KB) + wrap digest-body line-content strings in `DOMPurify.sanitize()` before dangerouslySetInnerHTML, OR extend the existing line-by-line parser to treat text content as plain strings (no HTML) and only preserve structure/styling. Owner confirmed: not a blocker for current work, but HIGH priority for a future security pass. |
| Medium (added 2026-07-02) | **Extend content-type-specific receipt formats beyond recipes** | Owner: "we already have smth similar" — referencing the Recipe-video receipt format item above (detector + domain-specific renderer branch). Owner wants the same pattern applied to other recognizable content types: date ideas, hobby ideas, beaches/pools, museums (i.e. "here's a place/activity you should know about" content, not just recipes). No detection/schema design done yet — owner said to just log the idea for now. Likely follows the same shape as the recipe item: cue-word detector → optional structured field on `save_nugget` → renderer branch, but for "recommendation venue/activity" content instead of ingredients/steps. |
| ✅ Shipped 2026-07-02 | **Digest web history page visual polish pass** | Gallery cards (`app/u/[token]/digests/page.tsx`) now reuse the `.nugget-card` hover/border treatment with a per-kind colored icon strip (weekly ◐ blue, monthly ◆ gold, yir ★ gold). Detail page (`app/u/[token]/digests/[id]/page.tsx`) no longer dumps raw `pre-wrap` text — new `lib/digest-body.tsx` (`renderDigestBody()`) parses the bot's fixed line vocabulary (border/separator rows, `🤖/🔥/⚡︎/🧠/📊/💡/⏳` section headers, `[bracket]` CTA lines, quoted insights split into per-sentence bullets, ` ▪ label: value` metrics, numbered clusters, year-in-review season bullets) into styled blocks matching the nugget detail page's "receipt" look (`text-[9px] uppercase tracking-[0.15em]` section labels, `<hr>` dividers). Falls back to plain paragraphs for unrecognized lines, so older/legacy-format digests (pre-2026-06-21 redesign) still render without breaking. New CSS classes in `globals.css` (`.digest-section-label`, `.digest-sep`, `.digest-quote`, `.digest-bullet`, `.digest-metric`, `.digest-cluster`, `.digest-season*`, `.digest-line`). Follow-up fix same day: the detail page's text wrapper used `text-gray-800 dark:text-gray-200` / `border-gray-200 dark:border-neutral-700` — Tailwind's `dark:` variant is driven by `prefers-color-scheme` (per this file's own globals.css comment), NOT the app's own `.dark` class toggle, so manually toggling dark mode while the OS is in light mode rendered near-black text on a now-dark background (invisible). Swapped for CSS-var-driven utilities (`text-foreground`, `border-border-muted`) that track the `.dark` class correctly. Also found + fixed in the same pass: `digest.py`'s `_finalize()` was truncating the body to Telegram's message-length budget *before* storing it via `save_digest_row`, so the web dashboard showed the same truncated "…(truncated — read the full report on the dashboard)" text with nowhere further to go. Split into `_finalize()` (always full/untruncated, used for DB storage) + new `truncate_for_telegram()` (applied only at the two `bot.send_message` call sites). Bot repo commit `009f0c7`. |
| Medium | **Duplicate detection** | Bot checks URL / image hash before processing. `[WARN] DUPLICATE_DETECTED` with existing entry id + date. Needs `content_hash` column + URL-normalized lookup. Saves Claude + Whisper tokens. |
| Medium | **Author hashtags from URL source** | Parse hashtags from yt-dlp description, pass to Claude as "optional author tags — include only if topically specific, ignore #fyp/#viral/#trending." Do NOT append raw (spam risk). |
| Medium | **Pin / favorite nuggets** | `pinned` boolean column. Pinned cards float to top. Simple schema + sort change. |
| Medium | **Share specific nugget** | Public `/n/[id]` permalink (no login). Bundle with Phase C magic URL. |
| Medium | **Web-only URL ingestion** (added 2026-06-28) | Let users paste a URL on the web dashboard without Telegram. Reuse the bot's pipeline via HTTP endpoint or port to Next.js API route. |
| Medium | **Note-taking on saved nuggets** | Add user annotation after the fact. `user_notes` text column. Edit UI on detail page. |
| Medium (added 2026-07-03) | **Reuse AI clusters (`extract_clusters`) somewhere beyond digests** | Currently only powers the monthly/yearly digest "top clusters" section (semantic grouping across folders, e.g. a FOOD recipe + LEARN article + GO video all landing in one "lisbon trip planning" cluster). Owner wants this surfaced elsewhere too — e.g. a "clusters" view on the web vault, or a way to browse nuggets by AI-detected theme instead of just folder/tag. No design done yet — just flagged as an idea during the 2026-07-03 digest-redesign brainstorm. |
| Medium (added 2026-07-02) | **Generic "can't process this" → SaveAsBot fallback hint** | Right now the SaveAsBot workaround pointer only exists for one specific rejection case (`TIKTOK_PHOTO`, `URLRejected` in `pipeline.py` → `bot.py` replies `[limit] TIKTOK_PHOTO — forward to @SaveAsBot, then send images back here`). Generalize this to every failure path that means "we can't fetch/process this ourselves" — e.g. Instagram photo posts blocked on `INSTAGRAM_COOKIES` not being set, any other `URLRejected`/`URLUnsupported` case, generic download/extraction exceptions. Instead of a bare `[err]`/`[unsupported]` reply, tell the user to open the link in **@SaveAsBot** (or forward the post to it) to pull the raw media/images out, then forward *that* result back into supernuggets, which supernuggets can ingest normally. Needs: audit every `[err]`/`[unsupported]`/`URLRejected` reply site in `bot.py`/`pipeline.py`, add a shared fallback-hint string, decide which failure modes it applies to (probably not every error — e.g. genuine transcription failures on a video we already downloaded fine aren't a SaveAsBot case) vs. ones where we simply couldn't fetch/access the source at all. |
| Medium | **Audio files** (`F.audio`) | Same Whisper pipeline as voice. May have title/performer/caption metadata worth prepending. |
| Medium | `AsyncAnthropic` + `httpx.AsyncClient` instead of `asyncio.to_thread` | Proper async throughout — removes the thread pool overhead. |
| Medium | Resize/compress originals saved in DB | Web grid shows nothing for image entries. Needs schema change. |
| Low | **Research mymind.com feature gap** (added 2026-06-28) | Audit what https://mymind.com/ can do that supernuggets can't — e.g. browser extension save, visual similarity search, automatic color/mood tagging, "spaces" organization, AI-powered rediscovery. Identify features worth porting to the bot or web dashboard. |
| Low | **Onboarding flow** | `/start` multi-step walkthrough — reduces first-day drop-off if bot is ever shared. 3 messages: "send a TikTok URL", "send a screenshot", "send a voice note". |
| Low | **Resurface enhancement** — "On This Day" | Same-date last year. Adds a memory/nostalgia dimension to the existing random resurface. |
| Low | **Animated GIFs** (`F.animation`) | Telegram delivers as silent mp4. Mostly silent → Whisper returns nothing. Pair with future vision-on-first-frame. |
| Low | **Video sent as document** | `F.document` with `mime_type` starting `video/` — common from Apple Photos "Save Original" mode. Route through video pipeline. |
| **Medium/High** (bumped 2026-07-02) | **Bulk video albums — observed live failure** | Multi-video albums via `media_group_id`. Originally scoped as "nice to have" (concatenated transcripts, `--- clip 2 ---`, `media_type="video_group"`), but now confirmed as an active bug: user sent a multi-video album (crochet tutorial clips) and got a separate `audio transcription failed / ping @holeden if it keeps happening` message per video instead of one combined entry. `bot.py`'s album debounce (`_flush_album`, keyed by `media_group_id`) only covers the **photo** path — `on_video` has no grouping, so each video in the album is processed independently by `on_video`, and transcription is failing for each one (possibly resource contention from N simultaneous Whisper/yt-dlp calls, not yet root-caused). Needs investigation before the fix: confirm whether failures are caused by the missing grouping itself (concurrent calls) or a separate transcription bug. |
| Medium (added 2026-07-03) | **>8-image sends split into two nuggets** | Telegram splits a single user send of more than 8 images into multiple separate messages (e.g. 11 images → 2 messages, each with its own `media_group_id`). `_handle_photo_batch`/`_flush_album` treats each `media_group_id` independently, so one intended post becomes two nuggets. Needs a burst-detector on top of the existing per-album debounce: if a new album from the same user arrives within a short window (few seconds) of the previous one flushing, buffer and merge before running the vision pipeline. Also logged in `~/supernuggets-bot/BACKLOG.md`. |
| Low | **Dark mode** (full) | BRAND.md §7 spec: Hemeon-orange-on-sepia. Effort: audit every component. Do after core features. |
| Low | **Full-text search** | Supabase FTS (`to_tsvector` + `to_tsquery`). Fine under ~500 entries with client-side; add when needed. |
| Low | **Bulk web operations** | Select + batch delete / move-to-folder. Real work — select mode, state management. After vault hits 500+ entries. |
| ✅ Done | **Source media preview on cards** | Shipped 2026-06-26 — thumbnails uploaded to Supabase Storage `thumbnails` bucket (public). `pipeline.make_thumbnail()` + `database.upload_thumbnail()`. Wired into direct photos + URL galleries. Frontend `parseSourceInfo` handles thumbnail-only entries. Remaining: backfill ~175 existing entries (backlog item), detail page preview. |
| Low | Add `media_count` column + persist Telegram `file_id`s | Web grid thumbnails. Schema migration. |
| ✅ Done | **Landing page footer tweaks** (added 2026-06-30) | Verified 2026-07-02 — already shipped same day it was added, backlog just wasn't updated. `app/page.tsx:227-255` has both the GitHub link (`⬈ github`) and `⬈ created by` (→ `https://t.me/holeden`). Commit `a82ab06`. |
| Low | Local OCR via Tesseract | Cuts vision cost 70-90% on screenshots. New dep. |
| ✅ Done | `delete_entry` returns True on 0 rows — multi-user gap | Verified 2026-07-02 — already fixed, not stale. `database.py`'s `delete_entry` uses `return=representation` and returns `True` only when Supabase echoes back a non-empty row list (`len(rows) > 0`), and `bot.py`'s `on_delete_yes` checks that boolean, showing `"can't delete — not yours or already gone"` on `False` instead of a false-success message. `git log -S"def delete_entry"` shows this check existed since the initial commit — backlog item was simply never marked resolved. |
| Low | `on_unsupported` matches service messages too | `bot.py` |
| ✅ Done | Module-level `httpx.Client` for connection reuse | Shipped 2026-05-27 in `database.py` |
| ✅ Done | **Basic PWA (manifest + icons)** | Shipped 2026-07-02 — see CHANGELOG.md. |
| Medium | **PWA offline shell** (added 2026-07-02) | Service worker (Workbox or hand-rolled) caching the app shell + last-viewed vault grid so it isn't a blank error offline. Needs `next-pwa` or a manual `sw.js` + registration in a client component. |
| Low | **PWA custom install prompt** (added 2026-07-02) | Capture Android's `beforeinstallprompt`, show a branded "Add to home screen" banner instead of the native browser menu. No iOS equivalent — Safari has no install-prompt API, would need a manual "tap Share → Add to Home Screen" hint. |
| Medium | **PWA push notifications** (added 2026-07-02) | Web Push (VAPID keys) for digest-ready / "on this day" notifications on the installed PWA. Pairs with the digest scheduler and resurface backlog items above. Needs the offline-shell service worker first, plus a `push_subscriptions` table and a backend trigger endpoint. |
| Low | **PWA maskable icon safe-zone fix** (added 2026-07-02) | `public/icon-maskable-512.png` reuses `telegram-bot-icon.png` as-is; sprite content bbox (363×417 in the 512 canvas) slightly exceeds the standard 80% maskable safe circle at the corners. Regenerate with more padding if a launcher visibly clips it. |
| Medium | **Highlight source folder + tags on nugget detail page** (added 2026-07-02) | Opening a nugget should highlight its folder in the left sidebar (same treatment as browsing that folder) and render its own tags in a visually distinct state on the detail page. Distinct from the tag-filter-link highlight above. |
| ✅ Done | **Inline title editing on nugget detail page** (added 2026-07-02) | Verified 2026-07-02 — already fully shipped, backlog just wasn't updated. `components/vault/title-editor.tsx` (pencil icon → edit → check/X confirm/cancel), wired into `app/u/[token]/n/[id]/page.tsx:160`. Server action `updateNuggetTitle` in `app/actions/nuggets.ts:82`, token-scoped with owner-check. |
| Medium | **Tag autocomplete with fuzzy match + recent-tags suggestion** (added 2026-07-02) | Typing a tag on the detail page should fuzzy-suggest existing similar tags (catches near-duplicates like `#cakedecor` vs `#cake_decor`). Clicking into an empty tag field (before typing) should suggest the ~5 most recently used tags. |
| ✅ Done (partial) | **Tag deduplication — prevention + safe backfill** (added 2026-07-04, shipped 2026-07-04) | Root-cause fix in the **bot**, not a web panel (admin panel was explicitly declined). Two layers in `pipeline.py` (commit `bf0198d`): **(1) prompt** — the established-tag addendum rewritten from a soft "prefer reusing" hint into a controlled-vocabulary mandate (copy existing spelling char-for-char, no separator/plural/typo variants, no two spellings of one concept per list); `ANCHOR_THRESHOLD` 3→2. **(2) deterministic normalizer** `normalize_tags()` runs after every model call (text/image/video) — separator-only fingerprint collapse (snaps `aitool`→`ai_tool`, drops intra-list dups, folds accents `jamón`→`jamon`), purely structural so it never conflates distinct words (`claud`≠`claude`, `remotion`≠`emotion`). Works per-user via their own tag history — no hardcoded vocabulary. **One-time backfill** applied the same safe logic to 8 existing entries (198/199/203/218/227/228/229/237). NOT done: semantic typos left in data (`claud`, `cloudcode`, `aitools`, `claudeskills`) — manual cleanup via the web tag editor. |
| Low | **"Did you mean?" merge chips on sidebar tag click** (added 2026-07-04) | Optional web-side complement to the bot-side dedup above. When a user clicks a tag in the sidebar, show fuzzy-near-neighbor chips ("merge `#catalunya` into `#catalonia`?") for the residual semantic dupes the deterministic normalizer can't safely auto-merge (typos, plurals, near-synonyms). Needs a fuzzy lib (e.g. `fuse.js`) + a token-scoped bulk-rename server action (extend the `updateNuggetTags` pattern in `app/actions/nuggets.ts` to sweep all of a user's rows). Human-in-the-loop by design — never auto-applies. |
| Medium | **Real 404 / 403 / 500 error pages** (added 2026-07-02) | Copy is already locked in BRAND.md §8 ("🎯 apply next time touching these routes") but none of the three are implemented — no `app/not-found.tsx`, no 403 handling, no `app/error.tsx` found in the repo. Needs: `app/not-found.tsx` (404, `[ ↩ RETURN TO VAULT ]` CTA), a 403 path for invalid/foreign magic-URL tokens (currently falls through to generic `notFound()` per `app/u/[token]/layout.tsx` — should distinguish "token doesn't exist" from "token valid but wrong content" if that distinction is even reachable), and `app/error.tsx` (500, client-side error boundary, `[ ↻ RETRY RE-LOAD ]` CTA). Straightforward once scoped — mostly wiring existing copy into Next.js's file-based error conventions. |

---

#### TIER — Mobile UI/UX polish (found in review, 2026-07-05)

| Priority | Item | Notes |
|---|---|---|
| ~~**HIGH**~~ | ~~**Search input triggers iOS auto-zoom**~~ | ✅ Shipped 2026-07-05 (commit `6693019`) — bumped to `text-[16px]`. |
| ~~**HIGH**~~ | ~~**Icon buttons ~26px, under 44px touch minimum — Delete is the risky one**~~ | ✅ Shipped 2026-07-05 (commit `6693019`) — all icon buttons now `w-11 h-11` (44px), Delete visually isolated with a divider. |
| ~~**HIGH**~~ | ~~**Tag-remove `×` button ~12×12px**~~ | ✅ Shipped 2026-07-05 (commit `6693019`) — padded hit area added. |
| ~~Medium~~ | ~~**Emoji icons in mobile nav, inconsistent with desktop's pixel-icon set**~~ | ✅ Shipped 2026-07-05 (commit `6693019`) — swapped for matching `pixel-icons.tsx` components. |
| ~~Medium~~ | ~~**Dead `BottomNav` component — better mobile nav pattern, never wired in**~~ | ✅ Shipped 2026-07-05 (commit `1326e8e`) — wired in as primary mobile nav; hamburger overlay shrunk to folders + digests + open bot; dropped the `//`-style labels (unused elsewhere). |
| Medium | **Easter-egg speech bubble breaks on small screens** | `components/vault/app-shell.tsx:378` hardcodes `left: 88px/232px` assuming desktop sidebar width. On a ~375px phone the bubble likely clips off-screen. Needs a mobile-safe position (e.g. `inset-x-4` below a breakpoint). |
| ~~Low~~ | ~~**No safe-area-inset handling anywhere**~~ | ✅ Shipped 2026-07-05 (commit `1326e8e`) — added `env(safe-area-inset-bottom)` padding to the new BottomNav. |
| Low | **Detail page section labels at 9px** | `app/u/[token]/n/[id]/page.tsx` — labels like "summary"/"receipts"/"tags" render at `text-[9px]`. Small for mobile reading; bump to 10-11px minimum. |
| Low | **Hardcoded gray-400/500 + manual `dark:` variants instead of semantic tokens** | Detail page (`app/u/[token]/n/[id]/page.tsx`) mixes `text-gray-400/500` + `dark:` variants throughout instead of the `--muted-foreground` token already defined in `globals.css`. Works today; future dark-mode contrast tuning has to happen in dozens of places instead of one token. |
| Low | **Dead code: `components/vault/search-header.tsx`** | Full folder-tab search header, unused — superseded by `AppShell`'s `HeaderSearch` + `MobileMenu`. Candidate for deletion (confirm truly unreferenced first). |
| Low | **"+tag" input fixed at `w-20` (80px)** | `components/vault/tag-editor.tsx:85` — cramped for typing a real tag on a phone keyboard. Not broken (browser scrolls the input), just tight. |
